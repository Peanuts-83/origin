// Output a single-linked list
let list = {
  value: 1,
  next: {
    value: 2,
    next: {
      value: 3,
      next: {
        value: 4,
        next: null,
      },
    },
  },
};

// loop version
function printList1(l) {
  let tmp = l;
  while (tmp) {
    console.log(tmp.value);
    tmp = tmp.next;
  }
}

// recursive version
function printList(l) {
  console.log(l.value);
  if (l.next){
    printList(l.next);
  }
}

printList(list);